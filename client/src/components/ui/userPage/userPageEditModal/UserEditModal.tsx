import React from 'react';
import type { UserType } from '../../../../types/authType';

type Props = {
  user: UserType;
};

export default function UserEditModal({ user }: Props): JSX.Element {
  return <div>UserEditModal</div>;
}
